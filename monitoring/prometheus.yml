global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: design-guardian-api
    static_configs:
      - targets:
          - host.docker.internal:3001   # local dev
          # - design-guardian.up.railway.app:443  # production (HTTPS)
    metrics_path: /metrics
    scheme: http   # passer à https pour Railway
